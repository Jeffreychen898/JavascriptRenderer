window.onload = () => {
	const config = {
		canvas: "canvas"
	};
	const renderer = new JSRenderer(config);
}

/*
const config = {
	canvas: "canvID",
};
const renderer = new JSRenderer(config);
renderer.draw.rect(50, 50, 100, 100, {color: white});
*/
